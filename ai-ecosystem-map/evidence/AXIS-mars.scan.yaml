repo: AXIS-mars
scanned: "2026-08-16"
session: CAND-SCAN-05
purpose: >
  "AXIS Engine" -- a separate, sibling game-engine implementation from
  CAND-SCAN-04's AXIS-ENGINE: constitutional/deterministic engine with a
  Rust core (18 forge-* crates, unsafe_code=forbid), native C++ Vulkan/OpenXR
  runtime, Qt6/PySide6 editor, shipping "Nova Exodus: Colony Nine" (Mars
  MMO). 4,898 files. NOT a byte-identical mirror of AXIS-ENGINE (different
  tech stack: Rust+C+++Qt6 here vs the other repo's stack) -- independently
  scanned.
files_read: 6
depth_note: >
  Same graphics-engine vocabulary-collision pattern as CAND-SCAN-04's
  AXIS-ENGINE (rendering pipeline, Vulkan compositor, mesh/terrain/shader
  code). Grep-only depth, sampled highest-signal paths (checked for a
  tools/ai/ or tools/oracles/ judge-harness pattern like AXIS-ENGINE's --
  NONE FOUND, this repo does not replicate that subsystem).
findings: {}
not_credited_adjacent_findings: >
  editor/studio-qt/neural_graphics_phase_b5_benchmark.py ("CAP.RG_NEURAL_PHASE_B5")
  is a real neural-graphics benchmark (DLSS-style temporal denoising +
  neural upscaling + supersampling for real-time rendering) -- genuine ML
  work, but computer-vision/image-upscaling ML, not a language model,
  embedding, agent, or any of the 89 target capabilities. Not credited.
empty_batteries:
  all_11: ["all 11 category batteries run; real-code hits confined to Vulkan/rendering/physics/animation engine internals -- no LLM/agent/RAG/MCP/vector-DB/AI-security/observability-of-AI-calls code found"]
vendor_hits: []
