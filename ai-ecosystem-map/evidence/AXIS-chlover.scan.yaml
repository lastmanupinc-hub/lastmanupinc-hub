repo: AXIS-chlover
scanned: "2026-08-16"
session: CAND-SCAN-08
purpose: >
  A library built around the Model Context Protocol -- packages/server is a
  from-scratch MCP server implementation (types, core dispatch, HTTP + stdio
  transports), packaged as a reusable library distinct from the ad-hoc
  in-app MCP servers found in other repos. 137 files.
files_read: 7
findings:
  mcp:
    - files: ["packages/server/src/McpServer.ts:1-227", "packages/server/src/protocol.ts", "packages/server/src/transports/http.ts", "packages/server/src/transports/stdio.ts", "spec.types.ts"]
      note: >
        A fifth independent hand-rolled MCP implementation (TypeScript,
        packaged as a standalone library rather than embedded in an app):
        tool/resource/prompt registration, JSON-RPC dispatch, capability
        negotiation, lifecycle management, dual transports (HTTP + stdio).
        No @modelcontextprotocol/sdk dependency (verified: package.json has
        no such dependency, no such import anywhere in the package) --
        genuinely built from the spec. Tested: McpServer.test.ts (225
        lines), schema.test.ts, http.test.ts.
      suggests_grade: peer
      slots: [MCP_MCP_SDK]
  llm: []
  agentic_ai: []
  rag: []
  embedding: []
  ai_security: []
  observability: []
  memory: []
  ai_agent: []
  automation: []
  vector_database: []
not_credited_adjacent_findings: >
  spec.types.ts is explicitly headed "Generated protocol contract types" --
  it is itself a scaffold artifact, but the McpServer.ts/protocol.ts/
  transports code that CONSUMES those types is real, hand-written,
  tested dispatch logic, not generated boilerplate -- credited on that
  basis. No registerTool()-style ergonomic decorator API found (checked for
  MCP_FASTMCP relevance) -- registration uses a different, non-decorator
  pattern; not separately credited to avoid diluting the already-strong
  MCP_FASTMCP bench grade from AXIS-Foundry with weaker evidence.
empty_batteries:
  all_other_10: ["all 11 category batteries run against the 137-file tree; only mcp yielded real capability code"]
vendor_hits: []
