###############################################################################
# AI_ECOSYSTEM_CAPABILITY_MATRIX.yaml — THE LEDGER
#
# SINGLE SOURCE OF TRUTH for how the lastmanupinc-hub portfolio grades against
# the 89 AI-ecosystem targets in ECOSYSTEM_TARGETS.yaml. The other governance
# files (CLAUDE.md, begin.yaml, continuation.yaml, SCAN_TREE.yaml) read from
# this matrix and never claim a grade it contradicts.
#
# Schema modeled on slate v1 (AXIS-ENGINE/slate/CAPABILITY_GAP_MATRIX.yaml)
# + AXIS-Foundry in-matrix change_log. Scoring doctrine: PEER-INDEPENDENCE —
# see ECOSYSTEM_TARGETS.yaml meta block (owner directive 2026-08-16, verbatim).
#
# AUTHORING RULES (lint-enforced by tools/gov_lint.py):
#   - Never grade above absent without at least one real file path in
#     peer_evidence (paths are verified to exist on disk at lint time).
#   - Never grade absent without recorded search_expressions.
#   - vendor_in_use NEVER justifies a grade above absent (dependence != peer).
#   - grade == partial requires missing_for_peer.
#   - Grades are restated, never overwritten: every change lands in change_log
#     with rows_touched. Upgrades require new file evidence; downgrades are
#     allowed any time with a reason.
#   - parity_100_criteria numbers are written ONLY by tools/rollup.py --write.
###############################################################################
meta:
  schema_version: 1
  created: "2026-08-16"
  schema_source: "slate v1 (AXIS-ENGINE/slate/CAPABILITY_GAP_MATRIX.yaml) + AXIS-Foundry change_log pattern"
  targets_source: ECOSYSTEM_TARGETS.yaml
  grade_enum: [peer, peer_plus, bench, partial, scaffold, absent, unaudited]
  grade_meanings:
    peer: "In-house capability fully replaces the vendor tool; end-to-end equivalence demonstrated (code + tests/verifier/wired call site)."
    peer_plus: "Exceeds the vendor on at least one axis, with cited evidence."
    bench: "Working in-house implementation exists and runs, but not proven end-to-end equivalent."
    partial: "Real code covers part of the capability; missing_for_peer names the rest."
    scaffold: "Only hints/stubs/design docs found; no working implementation read."
    absent: "Searched and not found; search_expressions recorded."
    unaudited: "Not yet scanned. Counts as 0 in all rollups."
  grade_weights: {peer: 1.0, peer_plus: 1.0, bench: 0.75, partial: 0.4, scaffold: 0.15, absent: 0.0, unaudited: 0.0}
  row_count: 89
  last_updated: "2026-08-16"

rows:
  LLM_OPENAI_GPT:
    category: llm
    slot: openai_gpt
    slot_label: "OpenAI GPT"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'language.?model|inference|tokenizer|logits|sampling|completion|transformer' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'language.?model|inference|tokenizer|logits|sampling|completion|transformer' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  LLM_ANTHROPIC_CLAUDE:
    category: llm
    slot: anthropic_claude
    slot_label: "Anthropic Claude"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'language.?model|inference|tokenizer|logits|sampling|completion|transformer' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'language.?model|inference|tokenizer|logits|sampling|completion|transformer' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  LLM_GOOGLE_GEMINI:
    category: llm
    slot: google_gemini
    slot_label: "Google Gemini"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'language.?model|inference|tokenizer|logits|sampling|completion|transformer' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'language.?model|inference|tokenizer|logits|sampling|completion|transformer' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  LLM_META_LLAMA:
    category: llm
    slot: meta_llama
    slot_label: "Meta Llama"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'language.?model|inference|tokenizer|logits|sampling|completion|transformer' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'language.?model|inference|tokenizer|logits|sampling|completion|transformer' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  LLM_MISTRAL_AI:
    category: llm
    slot: mistral_ai
    slot_label: "Mistral AI"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'language.?model|inference|tokenizer|logits|sampling|completion|transformer' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'language.?model|inference|tokenizer|logits|sampling|completion|transformer' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  LLM_COHERE:
    category: llm
    slot: cohere
    slot_label: "Cohere"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'language.?model|inference|tokenizer|logits|sampling|completion|transformer' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'language.?model|inference|tokenizer|logits|sampling|completion|transformer' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  LLM_HUGGING_FACE:
    category: llm
    slot: hugging_face
    slot_label: "Hugging Face"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'language.?model|inference|tokenizer|logits|sampling|completion|transformer' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'language.?model|inference|tokenizer|logits|sampling|completion|transformer' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  LLM_OLLAMA:
    category: llm
    slot: ollama
    slot_label: "Ollama"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'language.?model|inference|tokenizer|logits|sampling|completion|transformer' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'language.?model|inference|tokenizer|logits|sampling|completion|transformer' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  LLM_VLLM:
    category: llm
    slot: vllm
    slot_label: "vLLM"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'language.?model|inference|tokenizer|logits|sampling|completion|transformer' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'language.?model|inference|tokenizer|logits|sampling|completion|transformer' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  AGE_LANGGRAPH:
    category: agentic_ai
    slot: langgraph
    slot_label: "LangGraph"
    grade: partial
    peer_evidence:
      - {repo: AXIS-Foundry, files: ["engine/axis_foundry/ai/ai_agent.py:748-782", "engine/axis_foundry/ai/human_in_loop.py:1-45"], note: "Stateful plan->execute->evaluate->replan loop + real HITL review workflow wired into api_server.py (3 call sites) — 2 of LangGraph's 4 defining features", scan_ref: evidence/AXIS-Foundry.scan.yaml}
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'multi.?agent|orchestrat|agent.?loop|task.?graph|planner|delegat|handoff|swarm' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: "No node/edge graph structure (fixed linear action list) and no persistent/resumable checkpointing across process restarts — plan and review state are in-memory/per-request only."
    last_updated: "2026-08-16"

  AGE_CREWAI:
    category: agentic_ai
    slot: crewai
    slot_label: "CrewAI"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'multi.?agent|orchestrat|agent.?loop|task.?graph|planner|delegat|handoff|swarm' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'multi.?agent|orchestrat|agent.?loop|task.?graph|planner|delegat|handoff|swarm' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  AGE_MICROSOFT_AUTOGEN:
    category: agentic_ai
    slot: microsoft_autogen
    slot_label: "Microsoft AutoGen"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'multi.?agent|orchestrat|agent.?loop|task.?graph|planner|delegat|handoff|swarm' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'multi.?agent|orchestrat|agent.?loop|task.?graph|planner|delegat|handoff|swarm' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  AGE_MS_AGENT_FRAMEWORK:
    category: agentic_ai
    slot: ms_agent_framework
    slot_label: "Microsoft Agent Framework"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'multi.?agent|orchestrat|agent.?loop|task.?graph|planner|delegat|handoff|swarm' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'multi.?agent|orchestrat|agent.?loop|task.?graph|planner|delegat|handoff|swarm' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  AGE_LLAMAINDEX_WORKFLOWS:
    category: agentic_ai
    slot: llamaindex_workflows
    slot_label: "LlamaIndex Workflows"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'multi.?agent|orchestrat|agent.?loop|task.?graph|planner|delegat|handoff|swarm' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'multi.?agent|orchestrat|agent.?loop|task.?graph|planner|delegat|handoff|swarm' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  AGE_AWS_STRANDS_AGENTS:
    category: agentic_ai
    slot: aws_strands_agents
    slot_label: "AWS Strands Agents"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'multi.?agent|orchestrat|agent.?loop|task.?graph|planner|delegat|handoff|swarm' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'multi.?agent|orchestrat|agent.?loop|task.?graph|planner|delegat|handoff|swarm' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  AGE_CAMEL:
    category: agentic_ai
    slot: camel
    slot_label: "CAMEL"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'multi.?agent|orchestrat|agent.?loop|task.?graph|planner|delegat|handoff|swarm' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'multi.?agent|orchestrat|agent.?loop|task.?graph|planner|delegat|handoff|swarm' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  AGE_AGNO:
    category: agentic_ai
    slot: agno
    slot_label: "Agno"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'multi.?agent|orchestrat|agent.?loop|task.?graph|planner|delegat|handoff|swarm' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'multi.?agent|orchestrat|agent.?loop|task.?graph|planner|delegat|handoff|swarm' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  RAG_LANGCHAIN:
    category: rag
    slot: langchain
    slot_label: "LangChain"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'retriev|chunk|rerank|knowledge.?base|corpus|citation|grounding' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'retriev|chunk|rerank|knowledge.?base|corpus|citation|grounding' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  RAG_LLAMAINDEX:
    category: rag
    slot: llamaindex
    slot_label: "LlamaIndex"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'retriev|chunk|rerank|knowledge.?base|corpus|citation|grounding' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'retriev|chunk|rerank|knowledge.?base|corpus|citation|grounding' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  RAG_HAYSTACK:
    category: rag
    slot: haystack
    slot_label: "Haystack"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'retriev|chunk|rerank|knowledge.?base|corpus|citation|grounding' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'retriev|chunk|rerank|knowledge.?base|corpus|citation|grounding' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  RAG_DSPY:
    category: rag
    slot: dspy
    slot_label: "DSPy"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'retriev|chunk|rerank|knowledge.?base|corpus|citation|grounding' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'retriev|chunk|rerank|knowledge.?base|corpus|citation|grounding' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  RAG_RAGFLOW:
    category: rag
    slot: ragflow
    slot_label: "RAGFlow"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'retriev|chunk|rerank|knowledge.?base|corpus|citation|grounding' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'retriev|chunk|rerank|knowledge.?base|corpus|citation|grounding' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  RAG_GRAPHRAG:
    category: rag
    slot: graphrag
    slot_label: "GraphRAG"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'retriev|chunk|rerank|knowledge.?base|corpus|citation|grounding' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'retriev|chunk|rerank|knowledge.?base|corpus|citation|grounding' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  RAG_UNSTRUCTURED:
    category: rag
    slot: unstructured
    slot_label: "Unstructured"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'retriev|chunk|rerank|knowledge.?base|corpus|citation|grounding' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'retriev|chunk|rerank|knowledge.?base|corpus|citation|grounding' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  RAG_EMBEDCHAIN:
    category: rag
    slot: embedchain
    slot_label: "EmbedChain"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'retriev|chunk|rerank|knowledge.?base|corpus|citation|grounding' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'retriev|chunk|rerank|knowledge.?base|corpus|citation|grounding' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  EMB_OPENAI_EMBEDDINGS:
    category: embedding
    slot: openai_embeddings
    slot_label: "OpenAI Embeddings"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'embedding|cosine|vector_dim|semantic.?search|nearest.?neighbor|similarity' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'embedding|cosine|vector_dim|semantic.?search|nearest.?neighbor|similarity' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  EMB_COHERE_EMBED:
    category: embedding
    slot: cohere_embed
    slot_label: "Cohere Embed"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'embedding|cosine|vector_dim|semantic.?search|nearest.?neighbor|similarity' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'embedding|cosine|vector_dim|semantic.?search|nearest.?neighbor|similarity' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  EMB_VOYAGE_AI:
    category: embedding
    slot: voyage_ai
    slot_label: "Voyage AI"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'embedding|cosine|vector_dim|semantic.?search|nearest.?neighbor|similarity' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'embedding|cosine|vector_dim|semantic.?search|nearest.?neighbor|similarity' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  EMB_SENTENCE_TRANSFORMERS:
    category: embedding
    slot: sentence_transformers
    slot_label: "Sentence Transformers"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'embedding|cosine|vector_dim|semantic.?search|nearest.?neighbor|similarity' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'embedding|cosine|vector_dim|semantic.?search|nearest.?neighbor|similarity' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  EMB_BGE:
    category: embedding
    slot: bge
    slot_label: "BGE"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'embedding|cosine|vector_dim|semantic.?search|nearest.?neighbor|similarity' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'embedding|cosine|vector_dim|semantic.?search|nearest.?neighbor|similarity' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  EMB_VERTEX_EMBEDDINGS:
    category: embedding
    slot: vertex_embeddings
    slot_label: "Google Vertex AI Embeddings"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'embedding|cosine|vector_dim|semantic.?search|nearest.?neighbor|similarity' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'embedding|cosine|vector_dim|semantic.?search|nearest.?neighbor|similarity' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  EMB_AZURE_OPENAI_EMBEDDINGS:
    category: embedding
    slot: azure_openai_embeddings
    slot_label: "Azure OpenAI Embeddings"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'embedding|cosine|vector_dim|semantic.?search|nearest.?neighbor|similarity' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'embedding|cosine|vector_dim|semantic.?search|nearest.?neighbor|similarity' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  MCP_MCP_SDK:
    category: mcp
    slot: mcp_sdk
    slot_label: "MCP SDK"
    grade: peer
    peer_evidence:
      - {repo: AXIS-Foundry, files: ["engine/axis_foundry/mcp_server.py:1601-1740"], note: "Hand-rolled JSON-RPC 2.0 MCP server (initialize/tools-list/tools-call/shutdown), no SDK import, tested (tests/test_mcp_server.py)", scan_ref: evidence/AXIS-Foundry.scan.yaml}
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'mcp|jsonrpc|tools/call|tool_registry|server.?manifest|\\.well-known' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  MCP_FASTMCP:
    category: mcp
    slot: fastmcp
    slot_label: "FastMCP"
    grade: bench
    peer_evidence:
      - {repo: AXIS-Foundry, files: ["engine/axis_foundry/mcp_server.py:154-217"], note: "ToolDef/ToolParam ergonomic declarative tool registration with JSON-schema generation; dataclass-based, not decorator-based; no generic auth/composition layer", scan_ref: evidence/AXIS-Foundry.scan.yaml}
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'mcp|jsonrpc|tools/call|tool_registry|server.?manifest|\\.well-known' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  MCP_MCP_REGISTRY:
    category: mcp
    slot: mcp_registry
    slot_label: "MCP Registry"
    grade: peer
    peer_evidence:
      - {repo: AXIS-Foundry, files: ["engine/axis_foundry/mcp_server.py:1463-1481", "server.json", "tools/gen_mcp_server_json.py"], note: "build_registry_manifest() -> checked-in server.json + /.well-known/mcp.json route + drift-guard test; published to registry.modelcontextprotocol.io", scan_ref: evidence/AXIS-Foundry.scan.yaml}
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'mcp|jsonrpc|tools/call|tool_registry|server.?manifest|\\.well-known' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  MCP_GITHUB_MCP_SERVER:
    category: mcp
    slot: github_mcp_server
    slot_label: "GitHub MCP Server"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'mcp|jsonrpc|tools/call|tool_registry|server.?manifest|\\.well-known' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'mcp|jsonrpc|tools/call|tool_registry|server.?manifest' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  MCP_SLACK_MCP_SERVER:
    category: mcp
    slot: slack_mcp_server
    slot_label: "Slack MCP Server"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'mcp|jsonrpc|tools/call|tool_registry|server.?manifest|\\.well-known' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'mcp|jsonrpc|tools/call|tool_registry|server.?manifest' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  MCP_POSTGRES_MCP_SERVER:
    category: mcp
    slot: postgres_mcp_server
    slot_label: "PostgreSQL MCP Server"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'mcp|jsonrpc|tools/call|tool_registry|server.?manifest|\\.well-known' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'mcp|jsonrpc|tools/call|tool_registry|server.?manifest' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  MCP_GDRIVE_MCP_SERVER:
    category: mcp
    slot: gdrive_mcp_server
    slot_label: "Google Drive MCP Server"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'mcp|jsonrpc|tools/call|tool_registry|server.?manifest|\\.well-known' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'mcp|jsonrpc|tools/call|tool_registry|server.?manifest' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  MCP_FILESYSTEM_MCP_SERVER:
    category: mcp
    slot: filesystem_mcp_server
    slot_label: "Filesystem MCP Server"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'mcp|jsonrpc|tools/call|tool_registry|server.?manifest|\\.well-known' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'mcp|jsonrpc|tools/call|tool_registry|server.?manifest' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  SEC_NEMO_GUARDRAILS:
    category: ai_security
    slot: nemo_guardrails
    slot_label: "NVIDIA NeMo Guardrails"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'guardrail|prompt.?injection|pii|redact|content.?safety|refusal|sanitiz|admission.?gate' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'guardrail|prompt.?injection|pii|redact|content.?safety|refusal|sanitiz|admission.?gate' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  SEC_GUARDRAILS_AI:
    category: ai_security
    slot: guardrails_ai
    slot_label: "Guardrails AI"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'guardrail|prompt.?injection|pii|redact|content.?safety|refusal|sanitiz|admission.?gate' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'guardrail|prompt.?injection|pii|redact|content.?safety|refusal|sanitiz|admission.?gate' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  SEC_PRESIDIO:
    category: ai_security
    slot: presidio
    slot_label: "Microsoft Presidio"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'guardrail|prompt.?injection|pii|redact|content.?safety|refusal|sanitiz|admission.?gate' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'guardrail|prompt.?injection|pii|redact|content.?safety|refusal|sanitiz|admission.?gate' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  SEC_LAKERA_GUARD:
    category: ai_security
    slot: lakera_guard
    slot_label: "Lakera Guard"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'guardrail|prompt.?injection|pii|redact|content.?safety|refusal|sanitiz|admission.?gate' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'guardrail|prompt.?injection|pii|redact|content.?safety|refusal|sanitiz|admission.?gate' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  SEC_PROMPT_SECURITY:
    category: ai_security
    slot: prompt_security
    slot_label: "Prompt Security"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'guardrail|prompt.?injection|pii|redact|content.?safety|refusal|sanitiz|admission.?gate' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'guardrail|prompt.?injection|pii|redact|content.?safety|refusal|sanitiz|admission.?gate' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  SEC_PROTECT_AI:
    category: ai_security
    slot: protect_ai
    slot_label: "Protect AI"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'guardrail|prompt.?injection|pii|redact|content.?safety|refusal|sanitiz|admission.?gate' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'guardrail|prompt.?injection|pii|redact|content.?safety|refusal|sanitiz|admission.?gate' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  SEC_AZURE_CONTENT_SAFETY:
    category: ai_security
    slot: azure_content_safety
    slot_label: "Azure AI Content Safety"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'guardrail|prompt.?injection|pii|redact|content.?safety|refusal|sanitiz|admission.?gate' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'guardrail|prompt.?injection|pii|redact|content.?safety|refusal|sanitiz|admission.?gate' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  SEC_BEDROCK_GUARDRAILS:
    category: ai_security
    slot: bedrock_guardrails
    slot_label: "AWS Bedrock Guardrails"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'guardrail|prompt.?injection|pii|redact|content.?safety|refusal|sanitiz|admission.?gate' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'guardrail|prompt.?injection|pii|redact|content.?safety|refusal|sanitiz|admission.?gate' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  OBS_LANGSMITH:
    category: observability
    slot: langsmith
    slot_label: "LangSmith"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'trace|telemetry|eval.?harness|scorecard|prompt.?log|observab|audit.?log' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'trace|telemetry|eval.?harness|scorecard|prompt.?log|observab|audit.?log' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  OBS_LANGFUSE:
    category: observability
    slot: langfuse
    slot_label: "Langfuse"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'trace|telemetry|eval.?harness|scorecard|prompt.?log|observab|audit.?log' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'trace|telemetry|eval.?harness|scorecard|prompt.?log|observab|audit.?log' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  OBS_ARIZE_PHOENIX:
    category: observability
    slot: arize_phoenix
    slot_label: "Arize Phoenix"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'trace|telemetry|eval.?harness|scorecard|prompt.?log|observab|audit.?log' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'trace|telemetry|eval.?harness|scorecard|prompt.?log|observab|audit.?log' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  OBS_WANDB_WEAVE:
    category: observability
    slot: wandb_weave
    slot_label: "W&B Weave"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'trace|telemetry|eval.?harness|scorecard|prompt.?log|observab|audit.?log' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'trace|telemetry|eval.?harness|scorecard|prompt.?log|observab|audit.?log' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  OBS_TRULENS:
    category: observability
    slot: trulens
    slot_label: "TruLens"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'trace|telemetry|eval.?harness|scorecard|prompt.?log|observab|audit.?log' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'trace|telemetry|eval.?harness|scorecard|prompt.?log|observab|audit.?log' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  OBS_RAGAS:
    category: observability
    slot: ragas
    slot_label: "Ragas"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'trace|telemetry|eval.?harness|scorecard|prompt.?log|observab|audit.?log' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'trace|telemetry|eval.?harness|scorecard|prompt.?log|observab|audit.?log' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  OBS_PROMPTFOO:
    category: observability
    slot: promptfoo
    slot_label: "Promptfoo"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'trace|telemetry|eval.?harness|scorecard|prompt.?log|observab|audit.?log' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'trace|telemetry|eval.?harness|scorecard|prompt.?log|observab|audit.?log' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  OBS_HELICONE:
    category: observability
    slot: helicone
    slot_label: "Helicone"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'trace|telemetry|eval.?harness|scorecard|prompt.?log|observab|audit.?log' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'trace|telemetry|eval.?harness|scorecard|prompt.?log|observab|audit.?log' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  MEM_MEM0:
    category: memory
    slot: mem0
    slot_label: "Mem0"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'memory|session.?state|persist|ledger|recall|knowledge.?graph' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'memory|session.?state|persist|ledger|recall|knowledge.?graph' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  MEM_ZEP:
    category: memory
    slot: zep
    slot_label: "Zep"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'memory|session.?state|persist|ledger|recall|knowledge.?graph' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'memory|session.?state|persist|ledger|recall|knowledge.?graph' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  MEM_LETTA:
    category: memory
    slot: letta
    slot_label: "Letta"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'memory|session.?state|persist|ledger|recall|knowledge.?graph' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'memory|session.?state|persist|ledger|recall|knowledge.?graph' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  MEM_LANGGRAPH_MEMORY:
    category: memory
    slot: langgraph_memory
    slot_label: "LangGraph Memory"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'memory|session.?state|persist|ledger|recall|knowledge.?graph' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'memory|session.?state|persist|ledger|recall|knowledge.?graph' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  MEM_REDIS_MEMORY:
    category: memory
    slot: redis_memory
    slot_label: "Redis"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'memory|session.?state|persist|ledger|recall|knowledge.?graph' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'memory|session.?state|persist|ledger|recall|knowledge.?graph' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  MEM_POSTGRES_MEMORY:
    category: memory
    slot: postgres_memory
    slot_label: "PostgreSQL"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'memory|session.?state|persist|ledger|recall|knowledge.?graph' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'memory|session.?state|persist|ledger|recall|knowledge.?graph' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  MEM_NEO4J:
    category: memory
    slot: neo4j
    slot_label: "Neo4j"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'memory|session.?state|persist|ledger|recall|knowledge.?graph' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'memory|session.?state|persist|ledger|recall|knowledge.?graph' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  MEM_CHROMA_MEMORY:
    category: memory
    slot: chroma_memory
    slot_label: "Chroma"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'memory|session.?state|persist|ledger|recall|knowledge.?graph' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'memory|session.?state|persist|ledger|recall|knowledge.?graph' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  AGT_OPENAI_AGENTS_SDK:
    category: ai_agent
    slot: openai_agents_sdk
    slot_label: "OpenAI Agents SDK"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'agent.?framework|tool_use|function.?call|agent.?runtime|autonomous' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'agent.?framework|tool_use|function.?call|agent.?runtime|autonomous' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  AGT_LANGCHAIN_AGENTS:
    category: ai_agent
    slot: langchain_agents
    slot_label: "LangChain Agents"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'agent.?framework|tool_use|function.?call|agent.?runtime|autonomous' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'agent.?framework|tool_use|function.?call|agent.?runtime|autonomous' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  AGT_PYDANTIC_AI:
    category: ai_agent
    slot: pydantic_ai
    slot_label: "PydanticAI"
    grade: bench
    peer_evidence:
      - {repo: AXIS-Foundry, files: ["engine/axis_foundry/ai/ai_agent.py:1-810"], note: "Typed frozen-dataclass action I/O schemas + validate_action_input; model-agnostic; production callers cli.py:4217, api_server.py:9901; tests/test_ai_agent.py + test_agent_destubbing.py", scan_ref: evidence/AXIS-Foundry.scan.yaml}
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'agent.?framework|tool_use|function.?call|agent.?runtime|autonomous' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  AGT_SEMANTIC_KERNEL:
    category: ai_agent
    slot: semantic_kernel
    slot_label: "Semantic Kernel"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'agent.?framework|tool_use|function.?call|agent.?runtime|autonomous' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'agent.?framework|tool_use|function.?call|agent.?runtime|autonomous' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  AGT_GOOGLE_ADK:
    category: ai_agent
    slot: google_adk
    slot_label: "Google ADK"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'agent.?framework|tool_use|function.?call|agent.?runtime|autonomous' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'agent.?framework|tool_use|function.?call|agent.?runtime|autonomous' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  AGT_BEDROCK_AGENTS:
    category: ai_agent
    slot: bedrock_agents
    slot_label: "AWS Bedrock Agents"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'agent.?framework|tool_use|function.?call|agent.?runtime|autonomous' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'agent.?framework|tool_use|function.?call|agent.?runtime|autonomous' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  AGT_AZURE_FOUNDRY_AGENT:
    category: ai_agent
    slot: azure_foundry_agent
    slot_label: "Azure AI Foundry Agent Service"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'agent.?framework|tool_use|function.?call|agent.?runtime|autonomous' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'agent.?framework|tool_use|function.?call|agent.?runtime|autonomous' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  AUT_N8N:
    category: automation
    slot: n8n
    slot_label: "n8n"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'workflow|scheduler|cron|pipeline|orchestration|trigger|dag|queue|retry' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'workflow|scheduler|cron|pipeline|orchestration|trigger|dag|queue|retry' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  AUT_ZAPIER:
    category: automation
    slot: zapier
    slot_label: "Zapier"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'workflow|scheduler|cron|pipeline|orchestration|trigger|dag|queue|retry' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'workflow|scheduler|cron|pipeline|orchestration|trigger|dag|queue|retry' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  AUT_MAKE:
    category: automation
    slot: make
    slot_label: "Make"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'workflow|scheduler|cron|pipeline|orchestration|trigger|dag|queue|retry' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'workflow|scheduler|cron|pipeline|orchestration|trigger|dag|queue|retry' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  AUT_POWER_AUTOMATE:
    category: automation
    slot: power_automate
    slot_label: "MS Power Automate"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'workflow|scheduler|cron|pipeline|orchestration|trigger|dag|queue|retry' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'workflow|scheduler|cron|pipeline|orchestration|trigger|dag|queue|retry' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  AUT_TEMPORAL:
    category: automation
    slot: temporal
    slot_label: "Temporal"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'workflow|scheduler|cron|pipeline|orchestration|trigger|dag|queue|retry' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'workflow|scheduler|cron|pipeline|orchestration|trigger|dag|queue|retry' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  AUT_AIRFLOW:
    category: automation
    slot: airflow
    slot_label: "Apache Airflow"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'workflow|scheduler|cron|pipeline|orchestration|trigger|dag|queue|retry' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'workflow|scheduler|cron|pipeline|orchestration|trigger|dag|queue|retry' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  AUT_PREFECT:
    category: automation
    slot: prefect
    slot_label: "Prefect"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'workflow|scheduler|cron|pipeline|orchestration|trigger|dag|queue|retry' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'workflow|scheduler|cron|pipeline|orchestration|trigger|dag|queue|retry' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  AUT_KESTRA:
    category: automation
    slot: kestra
    slot_label: "Kestra"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'workflow|scheduler|cron|pipeline|orchestration|trigger|dag|queue|retry' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'workflow|scheduler|cron|pipeline|orchestration|trigger|dag|queue|retry' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  AUT_PIPEDREAM:
    category: automation
    slot: pipedream
    slot_label: "Pipedream"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'workflow|scheduler|cron|pipeline|orchestration|trigger|dag|queue|retry' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'workflow|scheduler|cron|pipeline|orchestration|trigger|dag|queue|retry' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  VDB_PINECONE:
    category: vector_database
    slot: pinecone
    slot_label: "Pinecone"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'vector.?store|hnsw|ann.?index|ivf|knn|pgvector|faiss|similarity.?index' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'vector.?store|hnsw|ann.?index|ivf|knn|pgvector|faiss|similarity.?index' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  VDB_WEAVIATE:
    category: vector_database
    slot: weaviate
    slot_label: "Weaviate"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'vector.?store|hnsw|ann.?index|ivf|knn|pgvector|faiss|similarity.?index' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'vector.?store|hnsw|ann.?index|ivf|knn|pgvector|faiss|similarity.?index' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  VDB_QDRANT:
    category: vector_database
    slot: qdrant
    slot_label: "Qdrant"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'vector.?store|hnsw|ann.?index|ivf|knn|pgvector|faiss|similarity.?index' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'vector.?store|hnsw|ann.?index|ivf|knn|pgvector|faiss|similarity.?index' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  VDB_MILVUS:
    category: vector_database
    slot: milvus
    slot_label: "Milvus"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'vector.?store|hnsw|ann.?index|ivf|knn|pgvector|faiss|similarity.?index' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'vector.?store|hnsw|ann.?index|ivf|knn|pgvector|faiss|similarity.?index' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  VDB_CHROMA:
    category: vector_database
    slot: chroma
    slot_label: "Chroma"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'vector.?store|hnsw|ann.?index|ivf|knn|pgvector|faiss|similarity.?index' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'vector.?store|hnsw|ann.?index|ivf|knn|pgvector|faiss|similarity.?index' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  VDB_PGVECTOR:
    category: vector_database
    slot: pgvector
    slot_label: "pgvector"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'vector.?store|hnsw|ann.?index|ivf|knn|pgvector|faiss|similarity.?index' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'vector.?store|hnsw|ann.?index|ivf|knn|pgvector|faiss|similarity.?index' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  VDB_ELASTICSEARCH:
    category: vector_database
    slot: elasticsearch
    slot_label: "Elasticsearch"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'vector.?store|hnsw|ann.?index|ivf|knn|pgvector|faiss|similarity.?index' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'vector.?store|hnsw|ann.?index|ivf|knn|pgvector|faiss|similarity.?index' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  VDB_REDIS_VECTOR:
    category: vector_database
    slot: redis_vector
    slot_label: "Redis"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'vector.?store|hnsw|ann.?index|ivf|knn|pgvector|faiss|similarity.?index' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'vector.?store|hnsw|ann.?index|ivf|knn|pgvector|faiss|similarity.?index' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

  VDB_MONGODB_ATLAS_VECTOR:
    category: vector_database
    slot: mongodb_atlas_vector
    slot_label: "MongoDB Atlas Vector Search"
    grade: absent
    peer_evidence: []       # [{repo, files: ["path:line"], note, scan_ref}]
    vendor_in_use: []       # [{repo, files, note}] — dependence record, informational only
    search_expressions: ["grep -rniE 'vector.?store|hnsw|ann.?index|ivf|knn|pgvector|faiss|similarity.?index' across CAND-SCAN-01 repos (lastmanupinc-hub, .github, Remedial-Action, axis-launch-community): 0 capability hits (prose-only or empty repos) — see evidence/*.scan.yaml", "CAND-SCAN-02 (AXIS-Foundry/A-eye/axis-launch): grep -rniE 'vector.?store|hnsw|ann.?index|ivf|knn|pgvector|faiss|similarity.?index' — no capability match (see evidence/AXIS-Foundry.scan.yaml empty_batteries/not_credited_adjacent_findings for what was found and why it did not qualify)"]
    repos_scanned: [lastmanupinc-hub, .github, Remedial-Action, axis-launch-community, AXIS-Foundry, A-eye, axis-launch]
    missing_for_peer: null
    last_updated: "2026-08-16"

# Newest-first. Every grade change lands here with rows_touched.
change_log:
  - date: "2026-08-16"
    title: "CAND-SCAN-02: scanned AXIS-Foundry, A-eye, axis-launch -- first real peer evidence surfaced"
    changes:
      - "AXIS-Foundry (1860 files, AI-native 3D foundry) yielded genuine peer-independence evidence: hand-rolled MCP protocol server + registry manifest (no SDK import, tested, production-wired) graded peer for MCP_MCP_SDK and MCP_MCP_REGISTRY"
      - "MCP_FASTMCP graded bench (dataclass-based ergonomic tool registration, not decorator-based)"
      - "AGT_PYDANTIC_AI graded bench (typed action-schema agent framework, model-agnostic, production-called from cli.py + api_server.py, tested)"
      - "AGE_LANGGRAPH graded partial (stateful plan/execute/replan loop + real human-in-the-loop review workflow, but no graph structure or cross-process checkpointing -- missing_for_peer recorded)"
      - "A-eye (rendering-architecture IP doc) and axis-launch (submission intake, no code) contributed no capability evidence -- confirmed absent, repos_scanned appended"
      - "All other AXIS-Foundry categories (LLM, RAG, Embedding, AI Security, Observability, Memory, Automation, Vector DB) verified absent for real capability code -- vendor dependence on xAI Grok / TRELLIS.2-Modal / RunPod recorded as vendor_in_use (never counted toward grade); adjacent-but-not-matching systems (security_config.py, audit_trail.py, refusal_policy.yaml, ai_quality_eval.py) documented in the evidence shard for future re-examination rather than credited under uncertainty"
    rows_touched: [MCP_MCP_SDK, MCP_MCP_REGISTRY, MCP_FASTMCP, AGT_PYDANTIC_AI, AGE_LANGGRAPH, PLUS_84_REPOS_SCANNED_APPENDED]
  - date: "2026-08-16"
    title: "CAND-SCAN-01 (pilot): scanned lastmanupinc-hub, .github, Remedial-Action, axis-launch-community — all 89 rows unaudited -> absent for this coverage"
    changes:
      - "Ran all 11 category batteries + dependence battery: 0 capability code hits (two repos are README-only, two are empty; the few prose hits describe OTHER repos and are not evidence)"
      - "All 89 rows: grade unaudited -> absent with per-category search_expressions recorded; repos_scanned = the 4 pilot repos"
      - "Honest floor stays 0.0% — expected under the peer-independence doctrine; evidence shards written for all 4 repos"
    rows_touched: [ALL_89]

# Numbers below are written ONLY by `python3 tools/rollup.py --write` and
# verified by `python3 tools/rollup.py --check` (gov_lint fails on divergence).
parity_100_criteria:
  audit_method: "python3 tools/rollup.py --check"
  last_run: "2026-08-16"
  current_counts: {peer: 2, peer_plus: 0, bench: 2, partial: 1, scaffold: 0, absent: 84, unaudited: 0}
  current_percentages:
    agentic_ai: 5.0
    ai_agent: 10.7
    ai_security: 0.0
    automation: 0.0
    embedding: 0.0
    llm: 0.0
    mcp: 34.4
    memory: 0.0
    observability: 0.0
    rag: 0.0
    vector_database: 0.0
  overall_percentage: 4.4
  coverage: {repos_scanned: 7, repos_total: 31, slots_audited: 89, slots_total: 89}
