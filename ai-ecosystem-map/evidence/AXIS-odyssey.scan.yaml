repo: AXIS-odyssey
scanned: "2026-08-16"
session: CAND-SCAN-06
purpose: >
  "AXIS Toolbox" -- a TypeScript monorepo (apps/api, apps/web, apps/cli,
  packages/*) that analyzes any codebase and emits 86-144 deterministic
  artifacts (CLAUDE.md, AGENTS.md, design tokens, MCP configs, etc.) --
  visible throughout this scan session as the "Generated by Axis Skills"
  footer on many CLAUDE.md files already read. 3,429 files.
files_read: 8
findings:
  mcp:
    - files: ["apps/api/src/mcp-server.ts:1-2641", "apps/api/src/server.ts:105,238,244-245,298-335"]
      note: >
        A fourth independent hand-rolled MCP implementation (TypeScript, distinct
        from AXIS-Foundry's Python and no-fate-payment-engine's 2 Go/TS ones):
        JSON-RPC 2.0 types, tools/list + tools/call dispatch, error
        categorization, intent logging. Production-wired: server.ts routes
        POST /mcp -> handleMcpEntrypoint -> handleMcpPost, and serves
        /.well-known/mcp.json + /mcp/.well-known/agent.json. LIVE hosted
        endpoint confirmed in this repo's own CLAUDE.md
        (axis-api-6c7z.onrender.com/mcp) -- not just code, an actual running
        service other repos in this portfolio reference as a real MCP server.
      suggests_grade: peer
      slots: [MCP_MCP_SDK, MCP_MCP_REGISTRY]
  llm: []
  agentic_ai: []
  rag: []
  embedding: []
  ai_security: []
  observability: []
  memory: []
  ai_agent: []
  automation: []
  vector_database: []
not_credited_adjacent_findings: >
  runSearchTools ("search_and_discover_tools", the free MCP tool advertised
  across many portfolio repos' CLAUDE.md files) is confirmed LEXICAL
  substring/token-matching search with hand-tuned scoring, not semantic or
  embedding-based -- not credited toward RAG or Embedding. The "144
  deterministic generators" (packages/generator-core) were checked
  specifically for real LLM API calls (grep for api.anthropic.com,
  api.openai.com, generativelanguage.googleapis.com -- ZERO hits anywhere in
  the repo) -- confirmed template/structural generation, matching the
  product's own "deterministic" branding; the generated *output* text
  mentions vendor names (e.g. producing CLAUDE.md files that say "Claude
  Code") but that is content being written, not an API call being made, so
  it is NOT recorded as vendor_in_use. packages/context-engine (structural
  repo-context extraction) has no embedding/cosine/vector code -- confirmed
  AST/structural, not semantic retrieval.
empty_batteries:
  all_other_10: ["all 11 category batteries run against the 3,429-file tree; only mcp yielded real capability code (above)"]
vendor_hits: []
